import { Component, inject } from '@angular/core';
import { FormsModule } from '@angular/forms';
import { Router, RouterLink } from '@angular/router';
import { LibraryService } from './library.service';
@Component({ standalone: true, imports: [FormsModule, RouterLink], template: `<h1>Register</h1><form (ngSubmit)="submit()" aria-labelledby="register-heading"><h2 id="register-heading">Kreiraj nalog</h2><label for="first-name">Ime</label><input id="first-name" name="firstName" [(ngModel)]="data.firstName" required><label for="last-name">Prezime</label><input id="last-name" name="lastName" [(ngModel)]="data.lastName" required><label for="email">Email</label><input id="email" name="email" type="email" [(ngModel)]="data.email" required><label for="username">Username</label><input id="username" name="username" [(ngModel)]="data.username" required><label for="register-password">Password</label><input id="register-password" name="password" type="password" [(ngModel)]="data.password" required aria-describedby="password-help"><p id="password-help" class="password-help" aria-live="polite">Lozinka mora imati najmanje 8 karaktera, veliko slovo i broj.</p>@if (data.password && !passwordStrong) {<p class="error-message" role="alert">Lozinka još nije dovoljno jaka.</p>} @if (passwordStrong) {<p class="success-message" role="status">Lozinka je dovoljno jaka.</p>}<button type="submit">Registruj se</button></form>@if (error) {<p class="error-message" role="alert">{{ error }}</p>}<a routerLink="/login">Nazad na login</a>` })
export class RegisterComponent { private library = inject(LibraryService); private router = inject(Router); data = { firstName: '', lastName: '', email: '', username: '', password: '' }; error = ''; get passwordStrong() { return this.data.password.length >= 8 && /[A-Z]/.test(this.data.password) && /\d/.test(this.data.password); } submit() { this.library.register(this.data).subscribe({ next: () => this.router.navigate(['/login']), error: error => this.error = error.error?.message ?? 'Registracija nije uspela.' }); } }